# 用户信息存储逻辑修改说明

## 修改内容

### 1. my.vue 页面修改
- 移除了 getUserInfo 的导入，因为现在直接从后端获取完整用户信息
- 修改了登录逻辑：通过 uni.login 获取 code，然后发送给后端
- 后端返回包含 token 和完整 userInfo 的数据
- 保存用户信息时，重新登录并传递修改后的用户信息给后端

### 2. profile.vue 页面修改
- 添加了 login 函数的导入
- 修改了保存逻辑：重新登录并传递完整的用户信息给后端更新

### 3. 新的数据流
1. **首次登录**：用户点击登录 → 获取微信 code → 发送给后端 → 后端生成 open_id 并查找用户 → 返回包含 token 和 userInfo 的数据
2. **更新信息**：用户修改信息 → 重新登录获取 code → 发送 code 和修改后的用户信息给后端 → 后端更新数据库 → 返回更新后的用户数据

## 后端接口要求

后端 `/login` 接口需要支持两种调用方式：

1. **首次登录**：`{ code: "微信登录code" }`
2. **更新信息**：`{ code: "微信登录code", userInfo: { nickName, avatarUrl, email, phone } }`

接口返回格式：
```json
{
  "code": 1,
  "data": {
    "token": "jwt_token",
    "userInfo": {
      "nickName": "用户昵称",
      "avatarUrl": "头像URL",
      "email": "邮箱",
      "phone": "手机号"
    }
  }
}
```

## 测试步骤

1. 确保后端接口已按照新格式实现
2. 运行应用，测试登录功能
3. 测试个人信息修改功能
4. 验证数据是否正确存储到数据库

## 注意事项

- 如果是第一次登录，后端返回的 userInfo 中 email 和 phone 字段可能为空
- 后端需要通过 code 生成 open_id 并在数据库中查找/创建用户
- 更新用户信息时，后端需要通过 code 找到对应用户并更新信息